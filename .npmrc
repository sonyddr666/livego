# Force npm to always install devDependencies regardless of NODE_ENV
# This ensures build tools like Vite are available in any environment
# Works on: Vercel, Render, Cloudflare Pages, Netlify, Coolify, Railway, Fly.io, etc.

# Never omit dev dependencies during install
include=dev

# Allow peer dependency conflicts
legacy-peer-deps=true
